n = input("Enter the number: ")
n = int(n)

count = 1
sum = 0

while count < n:
    if n % count == 0:
        sum = sum + count
    
    count += 1
    
print ("The sum of divisors of the number is %d" % sum)
